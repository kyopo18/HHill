using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UIElements;

public class RuckSack : MonoBehaviour
{
    // The RuckSack is purposely designed to make battle lighter and easier on the soldier.
    // Can only carry one spit ball at any time.
    // Can carry two items as a default
    // Is expandable to carry more.
    // Made of potato sack.
    // Weapons are not items.
    // Sidearms are not items.
    // Melee weapons are not items.
    // Carries one seasoning.

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    private void Item1()
    {
        // Holds Item One
    }

    private void Item2()
    {
        // Holds Item2
    }
}
