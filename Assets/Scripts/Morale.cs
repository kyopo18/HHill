using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// Morale is a mechanic similar to an anger meter.
// increased morale means increases in speed and fire rate
// Increased morale increases bravado as well.
// Bravado is maximum morale is similar to a 90's soldier film, an almost indestructible feeling an indestructible feeling is a boost in Flarmor.
// Activated Bravado buffs all stats for a short time but impairs vision in the character view a slightly thus affecting manual aim.

public class Morale : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    private void HotMeter()
    {
        // Increases a HotDog's move speed and shoot speed.
    }

    private void HamMeter()
    {
        // Increases a Burger's move speed and shoot speed.
    }
}
